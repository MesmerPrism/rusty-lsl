{
  "$schema": "https://github.com/MesmerPrism/rusty-morphospace-work-environment/schemas/iteration-unit.schema.json",
  "schema": "rusty.morphospace.workflow.iteration_unit.v1",
  "unit_id": "rlsl-core-005-descriptor-sample-binding",
  "project_id": "rusty-lsl",
  "status": "proposed",
  "objective": "Implement a dependency-free data-only binding between a validated stream descriptor and one homogeneous typed sample, with exact channel-count and channel-format checks plus explicit string-value bounds, while keeping conversion, encoding, wire, and runtime behavior outside the crate.",
  "tags": ["channels", "descriptor", "sample", "types"],
  "change_categories": ["implementation", "validation"],
  "instruction_impact": "update",
  "instruction_surfaces": [
    {
      "surface_kind": "agents",
      "path": "AGENTS.md",
      "owner": "rusty-lsl",
      "change_reason": "Route descriptor/sample binding invariants while keeping representation conversion, wire, and runtime surfaces closed.",
      "action": "update",
      "status": "planned",
      "validation": "powershell -NoProfile -ExecutionPolicy Bypass -File ./tools/check_all.ps1",
      "skill_id": null
    },
    {
      "surface_kind": "readme",
      "path": "README.md",
      "owner": "rusty-lsl",
      "change_reason": "Describe only the exact local descriptor/sample type and shape binding without claiming encoding, transport, protocol, or ecosystem support.",
      "action": "update",
      "status": "planned",
      "validation": "powershell -NoProfile -ExecutionPolicy Bypass -File ./tools/check_all.ps1",
      "skill_id": null
    },
    {
      "surface_kind": "skill",
      "path": "<skills-root>/system-engineering/SKILL.md",
      "owner": "workflow-maintainer",
      "change_reason": "Review whether homogeneous descriptor/data binding exposes a reusable contract rule; keep repo-specific primitive mappings out of the shared skill when existing contract guidance is sufficient.",
      "action": "update",
      "status": "planned",
      "validation": "Review the installed skill and run its instruction-surface whitespace check.",
      "skill_id": "system-engineering"
    }
  ],
  "instruction_none_justification": null,
  "prerequisites": ["rlsl-core-004-bounded-metadata-tree"],
  "allowed_repositories": [
    {
      "repo_id": "rusty-lsl",
      "allowed_paths": [
        "AGENTS.md",
        "README.md",
        "crates/rusty-lsl/src/",
        "docs/",
        "fixtures/compatibility/",
        "morphospace/",
        "tools/"
      ]
    }
  ],
  "non_scope": [
    "Numeric conversion, coercion, casting, parsing, formatting, byte sizing, encoding, decoding, endianness, wire discriminants, protocol format identifiers, or zero-copy layout claims.",
    "XML, metadata document assembly, discovery, resolution, networking, sockets, inlets, outlets, transport, buffering, queues, threads, clocks, scheduling, recovery, provider selection, FFI, native loading, or language bindings.",
    "Cargo dependencies, Cargo features, unsafe Rust, additional crates, dynamic dispatch plugins, generated protocol material, copied or translated liblsl or rLSL source, and production liblsl integration.",
    "Morphospace adapters, Manifold admission or stream authority, platform lifecycle, Quest or Hostess behavior, device validation, and any protocol, wire, runtime, performance, layout, or ecosystem compatibility claim."
  ],
  "acceptance": [
    {
      "acceptance_id": "homogeneous-descriptor-sample-binding",
      "proof": "One public unvalidated input family covers exactly f32, f64, String, i32, i16, i8, and i64 sample values; accepted state requires the matching data-only ChannelFormat and the descriptor's exact positive channel count without conversion or inference.",
      "command": "cargo test --workspace --all-targets --offline --locked"
    },
    {
      "acceptance_id": "sample-value-preservation-and-string-bounds",
      "proof": "Accepted numeric values preserve order and bits, accepted strings preserve text exactly, and an explicit nonzero per-string Unicode-scalar maximum rejects the first one-past channel with a stable indexed error.",
      "command": "cargo test --workspace --all-targets --offline --locked"
    },
    {
      "acceptance_id": "descriptor-sample-damaged-cases",
      "proof": "Focused tests cover all seven exact mappings, channel-count mismatch, each format mismatch family, zero string limit, exact and one-past Unicode-scalar bounds, empty strings, floating-point bit preservation, and stable typed error payloads.",
      "command": "powershell -NoProfile -ExecutionPolicy Bypass -File ./tools/check_core_005.ps1"
    },
    {
      "acceptance_id": "inert-core-closure",
      "proof": "The crate remains one unpublished std-only package with no dependencies, features, unsafe code, conversion, encoding, XML, runtime effects, protocol implementation, or compatibility overclaim.",
      "command": "powershell -NoProfile -ExecutionPolicy Bypass -File ./tools/check_all.ps1"
    }
  ],
  "risk_tier": "standard",
  "device_requirement": "forbidden",
  "validation": [
    {
      "profile_id": "source-only",
      "command": "powershell -NoProfile -ExecutionPolicy Bypass -File ./tools/check_all.ps1"
    },
    {
      "profile_id": "workflow",
      "command": "powershell -NoProfile -ExecutionPolicy Bypass -File <work-environment-root>/scripts/Test-WorkflowContracts.ps1 -WorkspaceRoot <project-root>/morphospace"
    }
  ],
  "outputs": [
    "Dependency-free descriptor/sample binding input, accepted-state, limits, and typed deterministic error APIs for exactly seven homogeneous value families.",
    "Positive and damaged tests plus a separate CORE-005 local-results overlay preserving every historical STRM-000 result.",
    "Deterministic CORE-005 validation entrypoint and public status updates that do not claim conversion, encoding, layout, protocol, transport, runtime, wire, or ecosystem support."
  ],
  "commit_policy": "Commit the accepted descriptor/sample binding separately from conversion, encoding, XML, discovery, protocol, transport, adapters, providers, or runtime work.",
  "push_checkpoint": "integration-batch"
}
