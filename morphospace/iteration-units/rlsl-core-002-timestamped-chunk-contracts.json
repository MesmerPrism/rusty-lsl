{
  "$schema": "https://github.com/MesmerPrism/rusty-morphospace-work-environment/schemas/iteration-unit.schema.json",
  "schema": "rusty.morphospace.workflow.iteration_unit.v1",
  "unit_id": "rlsl-core-002-timestamped-chunk-contracts",
  "project_id": "rusty-lsl",
  "status": "proposed",
  "objective": "Implement dependency-free typed raw and derived timestamp values plus bounded timestamped sample chunks while preserving every raw source timestamp and keeping clock computation outside the crate.",
  "tags": [
    "bounds",
    "chunks",
    "samples",
    "timestamps"
  ],
  "change_categories": [
    "implementation",
    "validation"
  ],
  "instruction_impact": "update",
  "instruction_surfaces": [
    {
      "surface_kind": "agents",
      "path": "AGENTS.md",
      "owner": "rusty-lsl",
      "change_reason": "Route timestamp and chunk contracts while keeping clock calculation, protocol, runtime, and external authority closed.",
      "action": "update",
      "status": "planned",
      "validation": "powershell -NoProfile -ExecutionPolicy Bypass -File ./tools/check_all.ps1",
      "skill_id": null
    },
    {
      "surface_kind": "readme",
      "path": "README.md",
      "owner": "rusty-lsl",
      "change_reason": "Describe the exact timestamp-preservation and bounded-chunk behavior implemented without promoting it to LSL runtime or interoperability support.",
      "action": "update",
      "status": "planned",
      "validation": "powershell -NoProfile -ExecutionPolicy Bypass -File ./tools/check_all.ps1",
      "skill_id": null
    },
    {
      "surface_kind": "skill",
      "path": "<skills-root>/system-engineering/SKILL.md",
      "owner": "workflow-maintainer",
      "change_reason": "Review whether preservation of raw observations beside labelled derived views exposes a reusable authority or evidence rule; avoid a repo-specific recipe when existing guidance is sufficient.",
      "action": "update",
      "status": "planned",
      "validation": "Review the installed skill and run its instruction-surface whitespace check.",
      "skill_id": "system-engineering"
    }
  ],
  "instruction_none_justification": null,
  "prerequisites": [
    "rlsl-core-001-bounded-contract-model"
  ],
  "allowed_repositories": [
    {
      "repo_id": "rusty-lsl",
      "allowed_paths": [
        "AGENTS.md",
        "README.md",
        "crates/rusty-lsl/src/",
        "docs/",
        "fixtures/compatibility/",
        "morphospace/",
        "tools/"
      ]
    }
  ],
  "non_scope": [
    "Reading any clock, computing clock correction, dejittering, smoothing, interpolation, sampling-rate-derived timestamps, timestamp replacement, or interpreting zero as an inlet/outlet sentinel.",
    "XML parsing or serialization, LSL protocol constants, wire framing or encoding, discovery, networking, sockets, process spawning, background threads, inlets, outlets, buffering, queueing, cancellation, recovery, or provider selection.",
    "Cargo dependencies, Cargo features, unsafe Rust, additional crates, generated protocol material, copied or translated liblsl or rLSL source, production liblsl integration, FFI, native-library loading, or language bindings.",
    "Morphospace adapters, Manifold admission or stream authority, platform lifecycle, Quest or Hostess behavior, device validation, and any wire, runtime, performance, or ecosystem compatibility claim."
  ],
  "acceptance": [
    {
      "acceptance_id": "raw-timestamp-preservation",
      "proof": "A timestamped sample always retains its finite caller-provided raw source timestamp unchanged; an optional derived value is separately typed and labelled and cannot replace or hide the raw value.",
      "command": "cargo test --workspace --all-targets --offline --locked"
    },
    {
      "acceptance_id": "bounded-chunk-contract",
      "proof": "Chunk construction makes maximum sample and channel counts explicit, preserves per-sample value/timestamp pairing and order, rejects inconsistent sample shapes, and returns typed deterministic errors before exposing accepted state.",
      "command": "cargo test --workspace --all-targets --offline --locked"
    },
    {
      "acceptance_id": "timestamp-chunk-damaged-cases",
      "proof": "Focused tests cover exact limits, one-past limits, non-finite timestamp rejection, inconsistent channel shapes, unchanged floating-point timestamp bits, raw-plus-derived coexistence, and stable error payloads aligned with the accepted STRM-000 semantic timestamp case.",
      "command": "powershell -NoProfile -ExecutionPolicy Bypass -File ./tools/check_core_002.ps1"
    },
    {
      "acceptance_id": "inert-core-closure",
      "proof": "The crate remains one unpublished std-only package with no dependencies, features, unsafe code, clocks, runtime effects, protocol implementation, or compatibility overclaim.",
      "command": "powershell -NoProfile -ExecutionPolicy Bypass -File ./tools/check_all.ps1"
    }
  ],
  "risk_tier": "standard",
  "device_requirement": "forbidden",
  "validation": [
    {
      "profile_id": "source-only",
      "command": "powershell -NoProfile -ExecutionPolicy Bypass -File ./tools/check_all.ps1"
    },
    {
      "profile_id": "workflow",
      "command": "powershell -NoProfile -ExecutionPolicy Bypass -File <work-environment-root>/scripts/Test-WorkflowContracts.ps1 -WorkspaceRoot <project-root>/morphospace"
    }
  ],
  "outputs": [
    "Dependency-free raw-source and labelled-derived timestamp value APIs with deterministic finite-value validation.",
    "A bounded timestamped sample-chunk contract that preserves per-sample order, values, and raw timestamp identity.",
    "Positive and damaged unit tests plus a separate CORE-002 compatibility overlay that does not rewrite the historical STRM-000 baseline.",
    "Deterministic CORE-002 validation entrypoint and exact public status updates that do not claim clock, protocol, runtime, wire, or ecosystem support."
  ],
  "commit_policy": "Commit the accepted timestamped chunk slice separately from clocks, protocol, runtime, networking, adapters, or provider work.",
  "push_checkpoint": "integration-batch"
}
