# P5–P6 Comparative Advisory Evidence

This frozen P45 lane adds one substantive crate-private, caller-requested comparative evidence owner over two actual P43 `CallerRequestedFloat32AdvisoryReportPackage` values and one actual P44 `MorphospaceFloat32AdvisoryReportPackageDeltaProposal`. The module remains intentionally undeclared and therefore has no public, runtime, activation, or ambient production surface.

The caller supplies a separate earlier package, a separate later package, the complete exact delta proposal, and an explicit nonzero fact bound. Successful composition consumes and retains all three inputs unchanged. In the proposal's existing fact order, it emits an earlier-side fact followed by a later-side fact. Each fact records the existing count kind, the selected P43 package total, the P44 fact's exact corresponding total, and exactly one checked relation from the package total to the proposal total: equal, increase, or decrease. Ties remain explicit. The proposal, including both packages and its ordered delta facts, is retained whole rather than cloned, reconstructed, or applied.

Construction validates representability, computes the required fact count with checked arithmetic, checks the bound before allocation, converts the count fallibly, reserves the complete allocation fallibly, and derives differences with checked subtraction. Zero, exact, one-past, allocation, conversion, arithmetic, and difference failures return the separate earlier package, separate later package, and complete proposal unchanged. No input is mutated and nested package, fact, evidence, and sample allocations retain identity; extraction consumes the evidence object and returns the same three inputs.

The comparison is bounded, default-inert, and non-applying. It derives facts only from exact totals already present in the supplied P43 packages and exact P44 proposal facts. It does not infer loss, continuity, causality, health, policy, or liblsl equivalence. It grants no public/runtime export and no Manifold, session, stream, transport, control, device, routing, admission, activation, authorization, or application authority.
