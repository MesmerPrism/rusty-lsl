# P5 Retained Comparative Snapshot Report

This P50 candidate adds one substantive crate-private, caller-requested report owner over one actual P49 `MorphospaceFloat32ComparativeAdvisoryEvidenceSnapshotDeltaHistory` and one actual P49 `CallerRequestedFloat32RetainedComparativeSnapshotPackage`. The report consumes and transactionally retains both complete owners unchanged. It neither clones nor reconstructs their P48 proposals, P47 snapshots, nested evidence, reports, samples, vectors, or other allocations, and consuming extraction returns both inputs.

The report derives only one compact deterministic exact evidence index. It records every retained snapshot-delta proposal in existing history order with its zero-based proposal/evidence index and exact existing fact count. It then records every retained package summary entry in existing order with its zero-based summary index and continuing evidence index: history entries retain the existing snapshot index and observation count, while delta entries retain the existing fact index and exact earlier/later counts. Equal counts remain exact ordered evidence. The report adds no ranking, tie policy, estimated loss, or continuity inference.

Three explicit nonzero bounds constrain history proposals, package summary entries, and total evidence entries. Configuration checks every bound's `usize` to `u64` representation. Construction checks both component extents, checked-adds the total, checks the total bound, fallibly converts the total plus every source and output index, and fallibly reserves the complete evidence index before emitting any entry. Every bound, arithmetic, conversion, or allocation failure owns and returns both complete P49 inputs with exact nested allocation identity. No partial report escapes.

The module remains private and undeclared after focused qualification. It is caller-requested, default-inert, advisory, and non-applying; it has no root/runtime export, activation/application path, or liblsl-equivalence claim. It enters no Manifold, session, stream, transport, control, routing, admission, authorization, device, oracle, or policy authority.
